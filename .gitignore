# Rendered files containing account IDs / ARNs / bucket names
*.rendered.json
env.sh

# Local outputs (structures encode the sequence — keep internal data out of git)
*.cif
*.metrics.json
results/

# Internal inputs / private campaign configs (target + antibody framework) — NEVER commit the dir.
# The committed design_config.example.json shows the config format with placeholders only.
Test_input/

# OS / editor
.DS_Store
__pycache__/
*.pyc
